/** Handles shown on the Rough notes card. No other survey fields. */
export type Socials = {
  insta?: string;
  tiktok?: string;
  snap?: string;
  web?: string;
};

export const SHIFA_SOCIALS: Record<string, Socials> = {
  S0053: {
    insta: "@ramzelekhtear",
    tiktok: "@ramzelekhtear",
    web: "rec.sa",
  },
  S0113: {
    insta: "@alqhtanicars",
    tiktok: "@groupalqahtani",
    web: "alqahtanicar.com",
  },
};
